name: C++ compiler checker

on: push

jobs:
  makefile_cpp_ex00:
    runs-on: ubuntu-latest
      steps:
      - name: Compiling c++ files
        run: |
          cd cpp00
          cd ex00
          make workflow
